name: cd 

on: 
  push: 
    branches: [main]

jobs: 
  deploy: 
    name: Deploy 
    runs-on: ubuntu-latest 

    steps: 
      - name: Check out code 
        uses: actions/checkout@v4

      - name: Set up Golang 
        uses: actions/setup-go@v5   # Updated to v5 to match your CI
        with: 
          go-version: "1.26.0"      # Keep this matching your CI Go version

      - name: Deploy Application
        run: |
          echo "Your deployment commands (Docker, AWS, GCP, etc.) go here!"

